# ============================================================
# TrayWave - Python dependencies
# Install with: pip install -r requirements.txt
# ============================================================

PyQt6>=6.5.0
PyQt6-Qt6>=6.5.0
PyQt6-Multimedia>=6.5.0
requests>=2.28.0

# ============================================================
# SYSTEM dependencies (Debian / Ubuntu / LMDE)
# Required for audio playback via Qt6 Multimedia (GStreamer backend)
#
#   sudo apt install \
#       gstreamer1.0-plugins-base \
#       gstreamer1.0-plugins-good \
#       gstreamer1.0-plugins-bad \
#       gstreamer1.0-libav
#
# Optional - tray icon support on some desktop environments:
#   sudo apt install libayatana-appindicator3-1
# ============================================================
